backup/
build/
doc/
.vscode/
deps/Glide2/
bin/
.commit*/
C2MenuAttempt/
carnivores_menu_mee/
.summaries/
CarnivoresPort/
Carnivores1/
stubs/
pkg/
# Local-only dev tooling: ignore everything under tools/ except the
# release packaging script (true allowlist — tools/release/* re-ignores
# any future helper files so only the script stays tracked).
tools/*
!tools/release/
tools/release/*
!tools/release/make-c2-package.ps1
